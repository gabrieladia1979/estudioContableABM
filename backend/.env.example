# Backend servidor de emails
PORT=4000

# Configuración SMTP (ej: SendGrid SMTP, Mailgun SMTP, o servidor SMTP propio)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password

# Email remitente y destinatario
FROM_EMAIL=web@tudominio.com
TO_EMAIL=tuemail@dominio.com
